// Typography

import 'package:micro_app_netflix_clon/tokens/typography_text.dart';

const h1Size = titleText;
const h2Size = subtitleText;
const h3Size = h3TextSize;
const textSelectedNavBar = selectedFontSizeText;
const textUnselectedNavBar = unselectedFontSizeText;
const iconSizeGeneral = sizeIcon;
const selectedSizeIcon = selectedIconSize;
const unselectedSizeIcon = unselectedIconSize;

